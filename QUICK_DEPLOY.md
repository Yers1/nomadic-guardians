# Быстрый деплой на Vercel (5 минут)

## Самый простой способ:

### 1. Загрузите код на GitHub

```bash
# Если git еще не инициализирован
git init
git add .
git commit -m "Initial commit"

# Создайте репозиторий на GitHub.com, затем:
git remote add origin https://github.com/ВАШ_USERNAME/nomadic-guardians.git
git branch -M main
git push -u origin main
```

### 2. Деплой на Vercel

1. Перейдите на **https://vercel.com**
2. Нажмите **"Sign Up"** → выберите **GitHub**
3. После регистрации нажмите **"Add New Project"**
4. Выберите ваш репозиторий `nomadic-guardians`
5. Vercel автоматически определит Next.js
6. Нажмите **"Deploy"**
7. Готово! 🎉

### 3. Ваш сайт будет доступен по адресу:
`https://nomadic-guardians.vercel.app` (или похожему)

---

## Что дальше:

- ✅ Каждый push в GitHub автоматически обновит сайт
- ✅ SSL сертификат включен автоматически
- ✅ Можно добавить свой домен в настройках
- ✅ Бесплатный план включает:
  - 100 GB bandwidth/месяц
  - Неограниченное количество проектов
  - Автоматические деплои

---

## Важно:

⚠️ **Данные спонсоров и файлы** будут храниться локально на сервере Vercel, но они **не сохранятся** между перезапусками на serverless платформе.

Для production рекомендуется:
- Использовать базу данных (MongoDB Atlas - бесплатно)
- Или облачное хранилище (Cloudinary - бесплатно для изображений)

Но для начала текущая версия будет работать!

